{%- if image != blank -%}
  {%- assign icon_src = image | img_url: '72x' -%}
  <span class="product-trust-points__icon-wrap" style="--trust-icon: url('{{ icon_src }}');" aria-hidden="true"></span>
{%- endif -%}
